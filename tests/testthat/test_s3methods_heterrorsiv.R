# Required data -------------------------------------------------------------------------------------------------------------------------------------------------------------------
data("dataHetIV")

# Run once to get result ----------------------------------------------------------------------------------------------------------------------------------------------------------
context("HetErrorsIV - S3 Methods")
# hetiv.input.form <- y~ P| X1 + X2 | X1 + X2 # needed as var to compare against
# expect_silent(res.hetiv <- hetErrorsIV(formula = hetiv.input.form, data = dataHetIV))
#
# test.s3methods.heterrorsiv(res.het=res.hetiv, input.form = hetiv.input.form, function.std.data=dataHetIV, req.df=9)
#
